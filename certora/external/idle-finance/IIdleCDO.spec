methods {
    function _.AATranche() external => NONDET;
    function _.BBTranche() external => NONDET;
    function _.token() external => NONDET;
    function _.virtualPrice(address) external => NONDET;
}